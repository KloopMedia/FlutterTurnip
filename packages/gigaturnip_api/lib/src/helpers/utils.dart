import 'package:gigaturnip_api/gigaturnip_api.dart';

// List<T> parseAndUnwrapData<T> (Map<String, dynamic> data) {
//   final parsedData = PaginationWrapper.fromJson(data);
//   return parsedData.results.map<T>((json) => T.fromJson(json)).toList();
// }